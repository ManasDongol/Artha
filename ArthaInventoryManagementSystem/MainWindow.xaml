<Window x:Class="ArthaInventoryManagementSystem.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ArthaInventoryManagementSystem"
        xmlns:controls="clr-namespace:ArthaInventoryManagementSystem.Controls"
        mc:Ignorable="d"
        Title="Artha Inventory"
        Height="700"
        Width="1200">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Top Bar -->
        <controls:Topbar Grid.Row="0"
                         Grid.ColumnSpan="2"/>

        <!-- Left Navigation -->
        <controls:Navbar Grid.Row="1"
                         Grid.Column="0"/>

        <!-- Dynamic Page Content -->
        <Frame x:Name="MainFrame"
               Grid.Row="1"
               Grid.Column="1"
               NavigationUIVisibility="Hidden"/>

    </Grid>

</Window>