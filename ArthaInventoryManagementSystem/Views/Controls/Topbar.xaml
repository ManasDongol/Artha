<UserControl x:Class="ArthaInventoryManagementSystem.Controls.Topbar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Border Background="White"
            BorderBrush="#D9D9D9"
            BorderThickness="0,0,0,1">

        <Grid Height="60">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- App Title -->
            <TextBlock Text="Artha Inventory Management"
                       VerticalAlignment="Center"
                       Margin="20,0"
                       FontSize="18"
                       FontWeight="SemiBold"
                       Foreground="#333333"/>

            <!-- User Section -->
            <StackPanel Grid.Column="1"
                        Orientation="Horizontal"
                        VerticalAlignment="Center"
                        Margin="0,0,20,0">

                <TextBlock Text="Welcome, Admin"
                           VerticalAlignment="Center"
                           Margin="0,0,10,0"
                           Foreground="#555"/>

                <Ellipse Width="32"
                         Height="32"
                         Fill="#E5E5E5"/>
            </StackPanel>

        </Grid>

    </Border>
</UserControl>